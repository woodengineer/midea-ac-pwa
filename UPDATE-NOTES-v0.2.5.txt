Midea AC PWA v0.2.5 — Dashboard Tile UI

UI-only dashboard cleanup:
- Removed the Control button from each AC tile.
- The entire AC tile now opens that unit's control page.
- Added keyboard activation (Enter/Space) and focus styling for the clickable tile.
- Removed the old bottom action row.
- Moved the Refresh icon to the far right of the Mode / set-temperature summary line.
- Refresh remains independent and does not open the unit page.
- Removed the text: "All control traffic stays on your local network."
- Discovery, manual add, REST endpoints, Temperature Unit handling, controller communication,
  and device detail controls are unchanged.
- Version/cache bumped to v0.2.5.
